[project]
name = "ormi-core"
version = "0.1.0"
description = "A C++ accelerated differentiable tensor engine."
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24.0",
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"