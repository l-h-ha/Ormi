[project]
name = "ormi-workspace"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[tool.uv.workspace]
members = ["ormi", "ormi-core"]

[tool.uv.sources]
ormi-core = { workspace = true }